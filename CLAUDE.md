# ourprochoi Research Archive — 작업 규칙 (v2 · 2026-09-13)

**투자 의사결정 파이프라인이다. 산출물은 페이지가 아니라 <u>판단</u>이다.**
**세 층이다(2026-09-13 사용자 결정)** — **`intake/` 수집**(무엇이 언제 왔나 · 원문 충실 · 판단 금지) → **`brain/` 판단**(그래서 어떻게 보나 · 내가 계속 갱신하는 정본) → **리서치 페이퍼**(왜 그렇게 보는지 · 사용자가 웹에서 읽는 뷰).
페이지 상단 「현재 판단 카드」는 브레인에서 렌더된다. 층마다 **쓰는 순간이 하나**다 — 수집은 도착 즉시, 브레인은 라우팅할 때, 페이지는 테제 상태가 바뀔 때.

```
사용자 뉴스·IB·방송 ─┐                     ┌→ theses · facts · entities · events · open
내가 받은 공시·XBRL·시세 ┴→ intake(id) → brain/routing(id→주장·등급·ⓐⓑⓒ) ┤
                                              └→ 테제 상태 변경 시 → 페이지 본문 갱신(카드는 자동)
```

| 꺼내 쓰는 파일 | 언제 |
|---|---|
| `intake/README.md` · `brain/README.md` | 두 층의 규칙. **처음 한 번** |
| `intake/user.jsonl` · `collected.jsonl` | **정보가 오면 먼저 한 줄.** 재등장 대조도 여기 grep. `files/`에 원문 조각. 자동 수집(`collect.py`)이 매일 붙인다 — `routed:false`인 것을 세션 시작 시 라우팅한다 |
| `brain/theses.json` | **종목·테마 질문을 받을 때 제일 먼저.** 페이지별 현재 판단(테제·근거·반증·판정 이벤트·상태). `card:true`면 페이지 카드가 여기서 나온다 |
| `brain/facts.json` | **수치를 인용·정정할 때.** 값·분자·분모·기준일·등급·산출 페이지 |
| `brain/routing.jsonl` | 라우팅 기록(intake id → 주장 단위 등급 → 어느 테제에 ⓐⓑⓒ). 「그때 무엇을 적었나」는 여기 |
| `brain/entities.json` · `events.json` · `open.json` | 회사 카드 · 판정 일정 · 열린 질문. **세션 시작 시 events·open을 읽는다** |
| `brain/regime.json` · `mechanisms.json` · `portfolio.json` | **v3(2026-09-15)** 시장 상태 · 인과 그래프 · 포트폴리오 테제. **세션 시작은 `regime.one`부터.** 시장·매크로·「어떻게 보나」 질문은 여기 먼저. 설계 `docs/workspace_v3.md` |
| `docs/thesis_index.md` | 87편 테제 색인(훅 생성). 카드 미전환 페이지를 찾을 때 |
| `docs/backlog.md` | 측정 명령 · 시작점 지표 |
| `docs/method_datasource.md` | 원문 경로(DART·SEC·13F·수급·환율·금리) · 파싱 함정 |
| `docs/rules_journal.md` · `docs/rules_archive_2026-09-13.md` | 규칙의 경위 · 종전 66조항 전문. **규칙을 고칠 때만** |
| `portfolio/portfolio_tracker.html` | 보유 수량·평단 (`HOLDINGS`) |

---

## J. 판단을 꺼낼 때 (out)

> **사용자와의 계약(2026-09-13 확정)** — *「이 아카이브의 목적은 나와 함께 투자 의사결정을 논의하는 것이다. 정보 정리는 수단이고, 산출물은 「그래서 어떻게 보는가」다.
> 확실해진 뒤에는 시장이 값을 주지 않으므로, <u>불확실할 때 방향을 말해 달라</u>. 다만 근거의 등급과 틀렸을 때 잃는 크기를 항상 붙여라.
> 판정 이벤트는 기다리는 시각표가 아니라 틀렸음을 빨리 아는 장치다. 사고 파는 집행과 수량·비중은 내가 정한다.
> 묻지 않은 포트폴리오 진단은 하지 말되, 반증 조건이 충족되거나 보유 종목의 판단이 바뀌면 묻지 않아도 즉시 알려라.」*

**J1. 검색보다 브레인 먼저.** `brain/theses.json`에서 해당 테제(카드 미전환이면 `thesis_index.md` → 페이지) → `facts.json`의 값 → 포트폴리오 편입 여부·평단 → 그래도 없으면 검색한다.
검색으로 답했고 **그 질문이 다시 올 것 같으면 그 자리에서 판단을 세운다**(커버리지는 그때만 오른다). 새로 찾은 값이 기존 판단과 다르면 답이 아니라 §R의 ⓑ 도전이므로 본문부터 고친다.

**J2. 답의 형식 — 종목·포지션·시점 질문에는 항상 이 다섯을 넣는다** (v3: 앞에 **⓪ 레짐 한 줄**(`regime.one`), 뒤에 **⑥ 포트 영향**(`portfolio.exposures`)을 붙인다).
① **기준일** ② **가격이 이미 전제하는 것**(배수 → 함의된 이익·성장, 분자·분모를 밝혀서) ③ **오늘 온 정보가 그 전제와 같은 방향인가 반대인가** — 어긋남이 기회이자 위험이다
④ **각 선택지의 대가** — 지금 움직이면 <u>무엇을 모른 채</u> 거는 것인지, 안 움직이면 무엇을 포기하는지(현금 보유는 중립이 아니다) ⑤ **근거 등급** ①공시 ②미확인 ③전문가 판단 ④수사 · `[추론]`.
**판정 이벤트는 「기다리는 시각표」가 아니라 「틀렸을 때 빨리 아는 장치」다.** 「불확실하니 보류」·「그때까지 기다리자」는 답이 아니다.

**J3. 하지 않는 것은 하나 — 집행 지시**(「N주 사라」·「X% 담아라」·「지금이 타이밍이다」). 이유는 아카이브 규칙이 아니라 **어시스턴트 쪽 제약**(공인 투자자문이 아니다)이며 이 파일을 고쳐도 움직이지 않는다.
**그 외에는 물으면 전부 준다** — 매매의 구조(무엇에 걸려 있나·틀렸을 때 잃는 크기·되돌릴 수 있나), 비중의 산술(분모를 밝힌 집중도·단일 반증에 걸리는 금액), 시점(판정 이벤트까지의 거리).
**먼저 꺼내지 않는 것도 하나 — 묻지 않은 포트폴리오 구성 진단.**

**J4. 묻지 않아도 즉시 말하는 것 넷** — 반증 조건 충족 · 판정 이벤트 도래 · 보유 종목의 페이지 판단 변경 · 스톱로스·매도선 도달.
판단이 바뀌면 **포지션 영향을 같은 답에서 세 줄로** — ① 무엇이 바뀌었나 ② 어느 테제·트리거에 걸리나 ③ 지금 필요한 행동(없으면 「없음」도 정식 결과).
수급·13F·환율·금리는 테제에 넣지 않고 **크기와 창을 밝혀 관측으로만** 적는다(방법은 `method_datasource.md`).

---

## R. 정보가 들어왔을 때 (in)

**R1. 주장 단위로 쪼개고 <u>각각</u> 등급을 매긴다.** 같은 출처·같은 날짜 안에서도 ①~④가 갈리고, 통째로 인용하면 ④가 ①의 무게를 얻는다.
**②는 버리지 않는다** — 「조건부」 칸에 <u>지금 단계 + 확정 조건</u>을 붙여 두되 확정과 더하지 않는다.

**R2. 어느 테제·반증 조건에 걸리는지부터 찾는다.** 순서는 **⓪ `intake`에 한 줄(id) → ① `intake`·`routing` grep(재등장이면 그때 적은 것과 대조) → ② 원문 조회(받은 조각은 `intake/files/`에) → ③ `routing`에 한 줄(intake id 부착) + 테제 `status`·`basis`·`last_tested` 갱신** — ②를 먼저 하면 ①은 실행되지 않는다. **페이지는 이 단계에서 열지 않는다.**

| 판정 | 처리 |
|---|---|
| **ⓐ 확인** | 테제 `log[]`에 날짜·등급·한 줄(2026-09-17 A). `basis`는 정본 문단이라 상태가 바뀔 때만 고친다. 새 블록 금지 |
| **ⓑ 도전** | **테제를 고친다**(status 도전·정정·철회). 급한 것은 이것뿐. **상태가 바뀐 페이지는 §W로 넘어간다** |
| **ⓒ 신규** | 배치처 있으면 병합, **없으면 버린다**(이유 한 줄) |
| **이미 판정** | 그때 적은 것을 grep으로 **대조한 뒤** 작업 0. 대조 없는 「이미 판정」은 회피다 |

**R3. 「그래서 어떻게 보는가」를 먼저 말하고 층 갱신은 뒤에 한 줄.** 커밋·타임라인·태그는 작업 내역이지 산출물이 아니다. 배치를 열기 전에 `brain/open.json`·`events.json`과 `intake`의 `routed:false`를 읽는다 — 안 읽었으면 그렇다고 적는다.

---

## W. 페이지를 쓸 때

**W1. 페이지는 「현재 상태」를 서술하는 뷰다 — 시간순 로그가 아니다.** 페이지를 여는 때는 넷 — **테제 상태가 바뀌었을 때 · 사용자가 페이퍼를 요청할 때 · 판정 이벤트를 처리한 뒤 · 한 페이지의 테제에 페이지 갱신일 이후 ⓐ 근거가 5건 쌓였을 때**(2026-09-16 사용자 결정 · 세는 명령은 `backlog.md`). 열면 쌓인 ⓐ를 해당 문단에 병합하고 basis의 날짜 꼬리표는 남긴다. hero 직후 **현재 판단 카드 1개**(브레인에서 렌더 · 테제 1~3개 기본·5개 상한 · 각각 근거·반증·판정 이벤트 한 줄 · **날짜 블록 금지**) → 본문(배경 지식 · 새 사실은 해당 문단에 병합) → **최근 변경 3~5건**(넘치면 footer 타임라인·`update_log`로) → 정정 이력.
테제 5개 초과 · 독립 결론 2개 이상 · 서로 다른 포지션 2개의 근거로 인용되면 **페이지를 분리**한다. 새 형식은 **다음에 손대는 페이지부터**(일괄 마이그레이션 금지).

**W2. 숫자는 (값 + 기준 + 시점) 세트다.** 배수·비율은 **분자와 분모를 한 줄로 적을 수 없으면 아직 값이 아니다** — 공개된 실측값을 먼저 찾고, 없을 때만 역산하며 역산값에는 「무엇을 역산했나」를 붙인다.
마진은 원문에서 분자·분모를 읽고 직접 나눈다(라벨 복사 금지). 분기 실적은 **확정인가 컨센서스인가**(`isConsensus`)를 먼저 본다. 같은 지표는 **정본 1곳** — 확정 지표는 `brain/facts.json`에 먼저 적고(어느 `intake` 조각에서 왔는지 함께) 페이지는 그 값을 쓴다(정정도 거기서 시작). 나머지 페이지는 참조 한 줄. 새 수치를 쓰기 전 `grep`으로 어디에 이미 썼는지 본다. 인물 직책·조직 역할은 재사용하지 않고 쓸 때마다 재확인한다.

**W3. 계정을 세기 전에 「이 계정이 사업을 반영하는가」를 묻는다** — 현금이 움직였나 · 반복되나 · 사업의 능력인가. 아니면 걷어낸 값을 **병기**하고, 한 계정으로 못 가르면 **짝이 되는 계정**을 연다(재고↔계약부채 · 영업이익↔매출 동행 · 잔고↔기초 잔고). **연결 지표가 세그먼트를 가리면 세그먼트를 연다.** 재무 성과(과거)와 비즈니스 질(GPM·ROIC·CapEx/매출·현금전환)은 같은 점수에 섞지 않는다.

**W4. 틀렸으면 본문을 고친다.** 「본문이 낡았다」고 적고 넘어가지 않는다 — 그 문장을 쓸 만큼 알면 고칠 만큼 안다. 못 고치면 페이지에 묻지 말고 막힌 것으로 보고한다.
정정은 **grep으로 후보를 좁힌 뒤 읽고 판정**하며 영향 반경(파생 결론 · 전제 테제 · 누락된 철회 · 미집행 액션)까지 따라간다. 취소선은 **틀렸던 것에만**(관점 대체는 본문 교체 + 타임라인 한 줄). **C1급**(당시에도 틀림 · 판단 영향) = 배너 + 별도 커밋 · C2급 노후화 = 한 줄 · C3급 추론 철회 = 회색 + 사유.

**W5. 「못 한다」고 적기 전에 시도한 <u>호스트·경로·티커</u>를 적는다.** 확인 안 한 것과 확인 못 하는 것은 다르고, 앞을 뒤처럼 적으면 영원히 검증되지 않는다. 미룬 확인은 `[미검증]`, 자체 추론은 `[추론]` + 반증 데이터 한 줄. 세계에 대한 서술·판단 완료·관측 대기 트리거에는 태그를 붙이지 않는다.

**W6. 끝낼 때** — 새 페이지는 `index.html` DATA(필드는 `category`·`file`·`type`; `cat`·`url`은 렌더러가 읽지 않는다 · 새 카테고리면 `SECTIONS`+CSS까지) + `sitemap.xml` + `update_log.html`을 **한 커밋**으로. `desc`는 120자 이내. 나머지 동기화는 훅이 한다(§H).

---

## E. 포트폴리오

**E1.** 구성·비중이 실제로 바뀔 때만 버전을 올리고 「무엇이 왜」 한 문장. 트리거를 만들면 다음 개정에서 **반드시 판정**하고 판정 불가면 폐기. 액션 지시는 **그 종목을 다음에 언급하는 순간** 집행 여부를 확인한다. 스톱로스를 오버라이드하면 재량 없는 매도선을 함께 정한다. 확률을 「유지」한다고 쓸 때 같은 문서에 신규 매크로를 나열했는지 본다.

---

## H. 도구 · 주기

**H1. 세 층 밖에 새 저장소를 만들지 않는다.** 정본은 `intake/` 둘 + `brain/` 아홉 파일(v2 여섯 + v3 셋 · **9가 상한**, 다음 구멍은 필드로)이고, 그 밖의 스크립트는 **층 사이 동기화와 자동 수집**뿐이다 — `.githooks/pre-commit`(`dateModified` · index `updated` · sitemap `lastmod` · `update_log` 동반 검사 · **카드 렌더** · **`thesis_index.md` 재생성** · **`facts` ↔ 페이지 값 대조** · **층 포인터 실재 검사**, 뒤의 둘은 경고만)과 `intake/collect.py`(매일 워크플로). 새 예외의 기준은 편의가 아니라 **실측 실패율**이다. 브레인이 죽지 않는 이유는 카드가 거기서 나오기 때문이다 — 낡으면 페이지 상단이 낡은 것이 보인다. 「차단됐다」고 적기 전에 호스트 단위로 확인한다(`dart.fss.or.kr`는 막혀도 `opendart`는 열려 있었다).
**주기 작업은 둘 — 주 1회 레짐 리뷰**(`regime.history`에 「같은 레짐인가」 한 줄 · 내러티브 정리) · **월 1회 감사 에이전트**(테제와 업데이트가 모순되는 페이지 · 정정 선언 후 살아 있는 원문 · 판정 없는 트리거 · 미집행 액션 · **v3: prediction 캘리브레이션 · mechanisms 부호 검산**. 개선안 금지, 측정된 사실만). 실행 기록은 `update_log`에 남긴다. **매일 해야 하는 것은 없다.** 지표는 손으로 세지 않고 `backlog.md`의 명령으로 센다. **판정 이벤트가 도래하면 판정하고 `events.json`에서 지운다** — grep은 후보를 좁힐 뿐 판정하지 않는다.

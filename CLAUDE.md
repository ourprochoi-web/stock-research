# ourprochoi Research Archive — 작업 규칙

## 이 저장소는 무엇인가

**투자 의사결정 파이프라인이다. 웹사이트가 아니다.**

```
정보 유입(스크랩·IB 리포트·방송) → 검증·분석 → 리서치 페이퍼 → 포트폴리오 반영
```

산출물은 페이지가 아니라 **판단**이다. 페이지는 그 근거를 담는 그릇이다.
현재는 개인 아카이브이며, 독자 확보는 향후 과제다. SEO·디자인은 후순위다.

---

## 0. 페이지의 목표 상태

**페이지는 "현재 상태"를 서술한다. 시간순 로그가 아니다.**
길이가 **시간이 아니라 정보량에 비례**해야 한다.

```
NAV   ▎현재 판단 | 01~0N 본문 | 최근 변경 | 정정 N
─────────────────────────────────────────────
▎현재 판단 · YYYY.MM.DD          ← hero 직후, 최상단 고정
   작성일 · 최종 갱신일 · 🔴정정 N · 🔵최근 N
   "한 문장 요약"
   T1~T5  (테제 3~5개, 각 3~4줄)
─────────────────────────────────────────────
▎반증 조건 · 다음 검증
   T2 → 무엇이 관측되면 이 테제는 틀렸다
   다음 검증 → 언제, 무엇으로 판정하는가
─────────────────────────────────────────────
01~0N 본문 = 배경 지식. 새 사실은 해당 문단에 병합
─────────────────────────────────────────────
▎최근 변경 (3~5건)  각 항목에 "→ T5 신설" 식 테제 연결
▎정정 이력          4단 구조 (아래 C4)
─────────────────────────────────────────────
그 이전 이력 → footer 타임라인 · update_log.html
```

**규칙**
1. **현재 판단은 페이지당 정확히 1개**, hero 직후. 테제 **3~5개**를 넘으면 → **페이지 분리**(D 참조)
2. 각 테제에 **반증 조건**이 없으면 테제가 아니라 감상이다
3. **다음 검증은 날짜가 아니라 이벤트**로 건다 — "9월 FOMC", "NVIDIA Q2". 날짜 리마인더는 40% 커밋 리듬에서 반드시 밀린다
4. **최근 변경은 3~5건 상한.** 넘치면 오래된 것을 지우고 footer 타임라인·update_log에만 남긴다 → **이것이 페이지 길이의 상한을 만든다**
5. 상단 테제 날짜 < 페이지 최신 업데이트 날짜 → **결함**. `grep` 한 줄로 검출 가능
6. **일괄 마이그레이션 금지.** 새 형식은 **다음에 손대는 페이지부터**. 업데이트가 안 쌓인 페이지는 모순도 안 쌓였다
7. 축소가 목적이 아니다. 업데이트 블록이 적은 페이지는 오히려 커질 수 있으며 **정상**이다

> 적용 완료(2026.08.08): 핫 6편 + **Tier 1 10편** = **16편**
> (메모리 사이클 · 빅테크 CAPEX · Q2 어닝 · AI SW 3사 · HBM 스코어보드 · AI 전력 ·
> 삼성전자 · SK하이닉스 · MSFT/팔란티어 · AI 전력 인프라 투자지도 · 노보/릴리 ·
> 로봇 부품 · 기판/소재 · 우주 2026 · 2차전지/ESS · AI 밸류체인 가이드)
> 남은 대상: **Tier 2 6편**(블록 2~4개) · **Tier 3 21편** — 규칙 6에 따라 **손댈 때** 전환

---

## A. 쓰기 전

**A1. 검증은 마지막이 아니라 처음이다.** 숫자를 쓰기 전에 확인한다. 검색 횟수에 상한을 두지 않는다 — 잘못된 판단의 비용이 검색 비용보다 훨씬 크다.

**A2. 마진·수익률은 원문에서 분자·분모를 함께 읽고 직접 나눈다.**
GM/OPM/NPM 라벨을 복사하지 않는다. **접두어 없는 마진 수치는 쓰지 않는다.**
> 실패 사례: SanDisk GM 84.6%를 OPM으로 오기. `$7,037M ÷ $8,965M = 78.5%`를 계산만 했으면 발생하지 않았다.

**A3. 인물 직책·조직 역할·매크로 프레임은 재사용 금지.** 쓸 때마다 재검색한다.
> 실패 사례: "Hassabis 전체 AI 연구 총괄"이 **전날** 사임으로 뒤집혔는데 그대로 기재.

**A4. 새 수치를 쓰기 전 `grep -rn "<수치>" .`** — 이미 어디에 썼는지 보고 정본을 정한다.
> 실패 사례: `$1.2T`가 29개 파일에 하드코딩. 하나 틀리면 29곳을 고쳐야 한다.

**「기준 미확인」이라고 쓰기 전에도 이 grep을 돌린다.** 확인하지 **않은** 것과 확인할 수 **없는**
것은 다르고, 전자를 후자처럼 적으면 그 자체가 오류다.
> 실패 사례(2026-08-08): 4,755조 vs 1,461조를 "3.3배 차이, 기준 미확인"으로 두 페이지에 적었다.
> 실제로는 `data/research_mega_project_3axes.md`에 **4,755조 = 삼성 2,655조 + SK 2,100조(민간 약정)**,
> `docs/지능제조업_정리.md`에 **1,461조+α = 정부 발표 금액**이 이미 있었다. 같은 것을 다르게 센 게
> 아니라 **서로 다른 돈**이었고, grep 한 번이면 나왔다. 그 사이 진짜 오류(국가 비교에 민간 약정을
> 넣어 한국을 3.3배 과대 표기)는 못 잡고 있었다.

**A7. 배수(PER·PBR·P/S·EV/EBITDA)는 <u>분모와 기준일</u>을 밝히지 않으면 쓰지 않는다.**

비율은 **분자·분모가 모두 움직이는데 겉으로는 하나의 숫자로 보인다.** 그래서 가장 조용히 틀린다.

- 페이지당 **밸류에이션 기준일을 1회 명시**하고 개별 언급은 그것을 상속한다(B5와 같은 구조).
- **같은 지표가 여러 페이지에 있으면 종목 페이지가 정본**이다. 나머지는 참조 한 줄.
- 배수가 어긋나면 **가격 변동으로 설명되는지 먼저 계산한다.** 안 되면 분모가 다른 것이다.

> 실패 사례 1 (2026-08-08): 한화에어로 `PER(26E) 31.7x`. 유통 EPS 45,694로 계산하면 주가가
> 1,448,957원이어야 하는데 실제는 1,097,000원으로 **32% 어긋났다.** 역산하면 EPS 32,829원이
> 필요해 **두 추정이 39% 벌어져 있었고**, 같은 주가에서 PER이 **24.0x ~ 33.4x**로 갈렸다.
>
**사이클 주식에서 PER은 방향이 뒤집힌다.** 이익이 바닥일 때 PER은 높고, 이익이 피크일 때
PER은 낮다. 그래서 **PER의 높낮이를 밸류에이션 판정에 그대로 쓰면 사이클 주식에서는 반대로
읽는다.** 배수를 보기 전에 **분모가 사이클의 어디에 있는지** 먼저 판정한다.

| | 배수 | 통상 해석 | 사이클 관점 실제 |
|---|---|---|---|
| 대한전선 | PER **133배** | "비싸다" | **이익이 바닥** — 성립 EPS 204원, 페이지 자체가 "OPM 5.6%로 마진 회복 도중"이라 기록 |
| SK하이닉스 | Forward P/E **3.5~7x** | "싸다" | **피크 선반영 가능** — 페이지가 Bear 논거로 쓰고 있음 |

> 두 사례가 **정확히 거울상**이다. 같은 아카이브 안에서 한쪽은 높은 PER을 과열 근거로,
> 다른 쪽은 낮은 PER을 피크 근거로 쓰고 있었다 — **둘 다 맞을 수 있지만 그러려면 분모의
> 사이클 위치를 명시해야 한다.** 명시가 없으면 편한 방향으로 읽게 된다.

> 실패 사례 2 (2026-08-08): SK하이닉스 `Forward P/E`가 아카이브 안에서 **3.5x / 6.4x / ~7x**
> 세 값으로 흩어져 있었다. **주가 변동으로는 설명되지 않는다** — 주가비는 1.46배인데 배수비는
> 2.00배다. 각 배수가 함의하는 2026E 순이익은 **296.8조 / 162.3조 / 148.4조로 2배 갈렸다.**
> 그중 `~7x`는 **Bear 논거("낮은 밸류에이션 = 피크 선반영")의 근거**로 쓰이고 있었다.

**A6. 외부 콘텐츠(방송·유튜브·전문가 인터뷰·비IB 리포트)는 등급을 먼저 매긴다.**

| 등급 | 정의 | 처리 |
|---|---|---|
| **① 확정 사실** | 공시·통계로 확인 가능하고 실제로 확인함 | 본문에 수치로 반영 |
| **② 검증 가능·미확인** | 구체적이라 확인은 되는데 아직 안 함 | 기록하되 **"미확인" 명시**, 투자 판단 근거로 쓰지 않음 |
| **③ 전문가 판단** | 반증 가능한 주장 | **반증 조건과 함께** 기록 |
| **④ 수사** | 규모감·비유 전달용 서술 | **근거로 쓰지 않는다** |

등급을 매긴 뒤 **기존 본문과 대조**한다 — ⓐ 확인 ⓑ 도전 ⓒ 신규 중 무엇인가.
**ⓑ 도전이면 본문을 고친다.** 아래에 블록을 붙이는 것은 처리가 아니다(B1·B6).

> 출처의 무게는 다르다. **공시 > IB 리포트 > 전문가 인터뷰 > 방송 요약** 순이며,
> 아래로 갈수록 ③·④ 비중이 커진다. 등급 없이 통째로 인용하면 **④가 ①의 무게를 얻는다.**

**A5. 수치는 (값 + 기준 + 시점) 세트로 쓴다.**
- ✗ `PER 219배` → ✓ `PER 219배 (2026E, 비경상 제외)`
- 같은 지표에 유효한 기준이 둘이면 **둘 다 병기**한다. 하나만 쓰면 반드시 나중에 충돌한다.

---

## B. 쓸 때

**B1. 기존 본문이 틀렸으면 본문을 고친다.** 하단 append는 본문 수정 실패다.

**B2. 상단 테제와 새 내용이 모순되면, 테제를 고치고 나서 커밋한다.**
> 실패 사례: 표본 5편 전원 테제가 6월 이후 편집 0회. 3편은 아래 업데이트와 직접 모순.
> 특히 `hbm_pkg_company_sk_hynix.html`은 같은 파일에서 "기준가 노후화"를 인정하고도 테제를 고치지 않았다.

**B3. 자체 추론은 `[추론]`으로 표시하고, 반증할 데이터를 한 줄 함께 적는다.**

**B4. 배치할 곳이 없으면 배치하지 않는다.** 이것도 정식 결과다.
> 모든 정보를 실어야 한다는 압력이 업데이트 블록 54% 비대화를 만들었다.

**B5. 섹터 전반 지표는 허브 1곳만 정본.** 나머지 페이지는 "→ 참조" 한 줄. 수치 복붙 금지.

**B6. "본문이 낡았다"고 쓰는 것을 금지한다.** — B1의 집행 규칙

편집자 주에 *"본 페이지 기준가 노후화"*, *"R3의 전제가 흔들렸다"*, *"이 수치는 ~월 기준이다"*
라고 적고 넘어가지 않는다. **그 문장을 쓸 만큼 알면 고칠 만큼 아는 것이다.**

> 왜 이 규칙이 필요한가: Tier 1 10편에서 **탐지는 한 번도 실패하지 않았다.** 실패한 모든
> 경우에 낡았다는 사실이 **이미 페이지에 적혀 있었다.** 알면서 안 고친 것이다 — 붙이는 쪽이
> 안전하고, 경고를 쓰면 일한 것 같은 기분이 들기 때문이다.
> `hbm_pkg_company_sk_hynix.html`은 하단에서 기준가 노후화를 경고하면서 본문 8곳을
> 그대로 뒀다.

그 세션에 못 고치면 **페이지에 묻지 말고 막힌 것으로 보고한다.** 페이지에 남긴 경고는
아무도 처리하지 않지만, 보고된 블로커는 처리된다.

---

## C. 정정 — **2단계로 한다**

**C1. ① `grep`으로 후보를 좁히고, ② 읽고 판단한다.**

`grep`은 **후보 축소 도구이지 판정 도구가 아니다.** 실측상 정정 6건 중 grep으로 온전히 잡힌 것은 2곳뿐이었다.

| grep이 잡는 것 | grep이 못 잡는 것 — **읽어야 안다** |
|---|---|
| 동일 문자열 (`84.6%`) | 같은 사실의 **다른 표현** ("NAND 마진 업종 최고") |
| | 그 수치에서 **파생된 결론** ("키옥시아를 상회") |
| | 그 사실을 **전제로 한 테제** |
| | **철회·정정이 누락된 곳** (부재는 검색되지 않는다) |
| | 그 판단에 **의존한 액션** (미집행 추적) |

**절차:**
1. `grep -rln "<틀린 값>" .` → 후보 파일 목록 (**untracked 포함**)
2. 후보 파일의 **상단 테제 + 관련 블록을 실제로 읽는다**
3. 파생 결론·전제 테제·누락된 철회를 판정한다
4. 전부 수정한다. **하나라도 남기면 정정이 아니다.**

> 실패 사례: `node-screener/…:433`이 untracked라 누락. 어떤 레지스트리도 못 잡았을 것이고, 잡을 수 있는 유일한 방법은 grep + 읽기였다.

**C2. 영향 반경을 판정한다.** "이 수치가 틀렸으면 어떤 결론이 무너지는가?"
> SanDisk OPM이 틀림 → "키옥시아를 상회" 서술 무너짐 → "NAND 슈퍼사이클 확장" 논거 약화. 여기까지 따라가야 정정이다.

**C3. 원문을 취소선으로 남기고 정정값을 병기한다. 삭제 금지.**
"그때 무엇을 알았는가"의 기록이 아카이브의 자산이다.

**단, 취소선은 「틀렸던 것」에만 쓴다.** 틀리지 않았는데 관점이 대체된 경우
(예: 테제의 적용 범위 축소, 분류 변경)는 **본문을 갈아끼우고 footer 타임라인에 한 줄**만
남긴다. 원문은 git에 보존된다.

> 이유: 실행할 수 없는 판단은 읽는 화면을 차지할 값어치가 없다. 바뀐 것을 전부 취소선으로
> 남기면 본문이 계속 길어져 §0의 "길이는 정보량에 비례" 원칙과 충돌한다.
> **읽는 화면은 유한하고 git은 무한하다** — 무한한 쪽에 보존한다.

**C4. 정정 등급을 구분한다.**

| 등급 | 정의 | 처리 |
|---|---|---|
| **C1급** 사실 오류 | 당시에도 틀렸음, 투자판단 영향 有 | 상단 배너 + **별도 커밋** |
| **C2급** 노후화 | 당시엔 맞았음 | 한 줄 추가, 배너 없음 |
| **C3급** 추론 철회 | 자체 추론이 반증됨 | 회색 처리 + 사유 |

전부 같은 무게로 처리하면 진짜 오류가 노이즈에 묻힌다.

---

## D. 페이지 분리

**크기(KB)는 기준이 아니다.** gzip 후 25KB, 컨텍스트 25K 토큰으로 문제없다.

**아래 중 둘 이상이 참이면 분리한다:**
- 상단 테제가 **독립된 결론 2개 이상**을 담는다 ("둘이 반대로 움직여도 말이 되는가")
- 업데이트가 원본 전제를 뒤집었는데 **원본을 못 고쳤다** (정정·무효 3건 이상)
- **서로 다른 포지션 2개 이상**의 근거로 인용된다

---

## E. 포트폴리오

**E1. 구성 또는 비중이 실제로 바뀌어야 버전을 올린다.** "전부 유지"면 날짜만 갱신.
> 실패 사례: 9개 버전 증분 중 **5개가 수치 변경 0**. v9.1·v9.2는 본문 섹션조차 없다.

**E2. 버전 변경 시 "무엇이 왜 바뀌었나" 1문장.** 못 쓰면 변경하지 않은 것이다.

**E3. 트리거를 신설하면 다음 개정에서 반드시 상태를 판정한다.**
판정 못 할 트리거는 만들지 않는다. 판정 불가로 확인되면 **폐기**한다.
> 실패 사례: 최소 13개가 정의 후 상태 판정 0회 (K1·K4·4-Factor 월간재계산·C1·C2·C3·C5·C6 등).

**E4. 액션 지시("즉시 매도" 등)는 다음 개정에서 집행 여부를 확인한다.**
> 실패 사례: SOL 매도 지시 8일 미집행. 그 사이 포지션 테이블에서 **종목 자체가 누락**.

**E5. 스톱로스를 오버라이드하면 무조건 매도 라인을 함께 정한다.**
재량 없는 가격선이 없으면 오버라이드는 그냥 방치다.

**E6. 확률(Bull/Bear)을 유지한다고 쓸 때, 같은 문서에 신규 매크로를 나열했는지 확인한다.**
> 실패 사례: v10.3이 FOMC 82%→54%·WTI +4%·10년물 4.68%를 나열하고도 "신규 매크로 없음"으로 확률 유지.

---

## F. 끝낼 때

**F1.** HTML 수정 시 그 파일의 JSON-LD `dateModified`를 오늘 날짜로 고친다. *(pre-commit 훅이 강제)*
**F2.** 새 페이지는 `index.html` DATA + `sitemap.xml`에 등록한다. 등록 안 하면 만들지 않은 것이다.
**F3.** 페이지 수정 + DATA + sitemap + update_log를 **한 커밋**으로. 쪼개면 드리프트가 생긴다. 단 **C1급 정정만 별도 커밋**(추적성).
*(pre-commit 훅이 강제 — footer에 `▸ 날짜`를 남기면 update_log 없이는 커밋이 막힌다)*
**F4.** index DATA의 `desc`는 **120자 이내**. 카드는 스캔용이지 초록이 아니다.
> 실패 사례: 최장 642자.

---

## G. 주기 작업

| 주기 | 작업 | 소요 |
|---|---|---|
| **페이지를 손댈 때** | 상단 테제 확인·갱신 | 3분 |
| **주 1회** | 트리거 전수 상태 판정, 판정 불가 시 폐기 | 20분 |
| **주 1회** | **내가 남긴 ⚠·미확인 표시 전수 판정** (아래) | 20분 |
| **월 1회** | 감사 에이전트 실행 (아래) | 40분 |

### 주 1회 — 미룬 검증 전수 판정

**규칙(B6)으로는 자제가 안 된다는 것이 실측으로 확인됐다.** 2026-08-08에 B6("본문이
낡았다고 쓰지 말고 고쳐라")을 쓰고 **같은 날 한화에어로 밸류에이션에 "⚠ 현행화되지 않음"을
적고 넘어갔다.** 사용자가 밀지 않았으면 그대로 남았을 것이고, 검증해 보니 **가격이 아니라
실적이 통째로 틀려 있었다**(컨센 36% 상회를 "부합"으로).

그래서 자제가 아니라 **세는 것**으로 잡는다. 다만 **무엇을 세는지가 중요하다.**

> **⚠ 첫 시도는 실패했다.** 처음에는 `미확인|판정 불가|무효로 둔다` 를 grep 해 **23건**을
> 기준선으로 잡았는데, 세어 보니 대부분이 **미룬 일이 아니라 서술**이었다 —
> *"Meta 수익성 미확인"*(분석 판단), *"중국은 NMPA 미승인으로 진입 미확인"*(사실 서술),
> *"확인 221조 + 미확인 149조"*(문서화된 내역), *"원인 미확인"*(의도적 단정 회피).
> **중복까지 빼면 실제로 미룬 것은 4건이었다.** 지표가 5배 이상 부풀어 있었다.

**미룬 검증에는 `[미검증]` 태그를 붙인다.** `[추론]`(B3)과 같은 방식이고, 이유는 같다 —
**서술과 미룬 일은 문장만 봐서 구별되지 않기 때문**이다.

```
grep -rc '\[미검증\]' --include='*.html' . | grep -v ':0$'
```

**그리고 「못 한다」고 적은 것부터 시도한다.**

오늘(2026-08-08) **같은 실수를 두 번** 했다. 둘 다 **시도조차 하지 않고** 불가능하다고 적었고,
**둘 다 해보니 됐다.**

| 내가 적은 것 | 실제로 필요했던 것 | 결과 |
|---|---|---|
| "4,755조 vs 1,461조 — **기준 미확인**" | `grep` 한 번 | 두 문서에 **이미 정의돼 있었다** |
| "해외 시세는 **확보할 수 없어** 검증 불가" | 엔드포인트 하나 바꿔보기 | **팔란티어 기준가 +36.9% 괴리** 발견 |

두 번째가 특히 비쌌다 — 그대로 뒀으면 **BofA TP 업사이드가 +103%로 남고**(실제 +48.2%),
**T3의 "섹터 디레이팅" 논지가 반대 증거를 못 만난 채** 유지됐을 것이다.

> **확인하지 않은 것**과 **확인할 수 없는 것**은 다르다. 전자를 후자처럼 적는 순간
> **그 항목은 영원히 검증되지 않는다** — "불가능"이라고 적힌 것을 다시 시도하는 사람은 없다.
> 그래서 **불가 판정에는 "무엇을 시도했는지"를 반드시 함께 적는다.** 시도 기록이 없는 불가
> 판정은 판정이 아니라 회피다.

```
grep -rn '확보하지 못\|확인할 수 없\|검증 불가\|불가능\|산출 불가\|역산 불가' --include='*.html' .
```

세는 대상은 **내가 확인할 수 있었는데 안 한 것**만이다. 다음은 세지 않는다.
- **세계에 대한 서술** — "중국 NMPA 미승인", "Meta 수익성 미확인"
- **의도적 단정 회피** — "원인 미확인(A vs B vs C)" 처럼 근거가 없어 단정을 피한 것
- **반증 조건이 붙은 ③ 전문가 판단** — 이미 관리되고 있다
- **관측 대기 중인 트리거** — "사우디 계약 미체결"은 판정된 상태이지 미룬 것이 아니다

> **왜 이게 다른 지표보다 중요한가**: 현재 판단이 붙은 페이지는 T1~T5와 반증 조건 때문에
> **엄격해 보인다.** 그 안의 숫자가 검증되지 않았다면 **형식이 오류를 가려주는** 셈이다.
> 페이지 수를 늘리는 것(Tier 3)보다 이미 만든 것을 믿을 수 있게 만드는 쪽이 먼저다.

**매일 해야 하는 것은 없다.** 커밋률 40%(최대 12일 공백) 리듬에서 일일 루틴은 반드시 깨지고, 밀린 규칙은 나머지 규칙의 신뢰도까지 떨어뜨린다.

### 월 1회 감사 에이전트

grep으로 못 잡는 것을 찾기 위해 서브에이전트를 돌린다. 실제로 6건의 미정정·모순이 이 방식으로 발견됐다.

```
저장소를 조사해 다음을 찾아라:
1. 상단 테제와 하단 업데이트가 모순되는 페이지
2. 정정이 선언됐으나 원문이 살아 있는 곳 (untracked 포함)
3. 정의 후 상태 판정이 없는 트리거
4. 지시됐으나 집행 확인이 없는 액션
개선안 금지, 측정된 사실만 보고.
```

---

## H. 금지

**새 도구·스크립트·레지스트리·인덱스를 만들지 않는다.**
전파 도구는 `grep` + **읽고 판단하기**다.

방치 선례 — 전부 "사람이 안 보는 구조"였다:

| 구조 | 정지 시점 | 상태 |
|---|---|---|
| `data/ground_truth.yaml` | 2026-07-25 | **커밋조차 안 됨** (제안됐던 "테제 객체"와 사실상 동일) |
| `data/research.json` | 2026-06-23 | 42편 등록 / 실제 112편 |
| `llms.txt` | 2026-06-28 | 20편 누락 |
| `update-prices.yml` | 2026-07-16 | 3주간 커밋 0건 |
| `node-screener/src` | 2026-07-27 | 전부 untracked |

살아남은 것은 `index.html`·`sitemap.xml` 둘뿐이고, 차이는 스키마 품질이 아니라 **사람이 매일 보느냐**였다.

**예외 3개만 인정한다.** 셋 다 "지시로는 실패한다"가 실측으로 확인된 것들이고,
셋 다 **새 데이터 저장소를 만들지 않는다** — 기존 파일의 동기화만 강제한다.
- `dateModified` 갱신 pre-commit 훅 (지시로는 78% 실패 — 기계적 동기화는 강제만 작동)
  → **구현됨**: `.githooks/pre-commit`, `git config core.hooksPath .githooks`.
  **내용이 바뀐 커밋에만** 날짜를 찍는다. 날짜 줄만 바뀐 커밋까지 오늘로 찍으면
  "날짜를 고쳤으니 갱신일이 오늘"이라는 순환이 되어, 6월에 쓰고 손대지 않은 글이
  오늘 갱신된 것처럼 보인다. 부분 스테이징된 파일도 건드리지 않는다.
- `update_log.html` 동기화 **검사** (지시로는 6/6 실패 — §F3을 쓴 다음 날 Tier 1 커밋
  6개에서 전부 누락했다)
  → **구현됨**: 같은 훅. 페이지 footer에 `▸ 날짜` 항목이 **새로 붙었는데** `update_log.html`이
  함께 스테이징되지 않으면 **커밋을 막는다.** `--no-verify`로 우회 가능.
  **훅이 내용을 쓰지는 않는다** — 무엇이 왜 바뀌었는지는 판단이 필요하고, 판단을 생성하는
  스크립트가 곧 §H가 금지하는 것이다. 훅은 "빠졌다"만 말한다.
- `.gitignore` 정리 (untracked 파일을 grep 범위에 포함시키기 위해)

### 이력은 어디에 남는가 — 층마다 역할이 다르다

한때 이력이 4군데에 흩어져 서로 맞지 않았다(취소선 3편 / 정정 이력 6편 /
footer 타임라인 21편 / update_log 8편). 층을 늘리지 말고 **역할을 고정**한다.

| 층 | 무엇을 담나 | 상한 |
|---|---|---|
| **현재 판단 + 최근 변경 + 정정 이력** | 지금 행동에 쓰는 것 | 테제 3~5 · 변경 3~5건 |
| **footer 타임라인 `▸`** | 이 페이지가 언제 무엇 때문에 바뀌었나 (**페이지별 정본**) | 없음 |
| **`update_log.html`** | 주 단위 크로스 페이지 뷰 (독자용) | 없음 |
| **git** | 원문 전체 | 무손실 |

죽는 층은 언제나 **다른 파일에 한 번 더 써야 하는 층**이다. 그래서 update_log만 훅으로
막고, 나머지는 전부 같은 파일 안에 둔다.

---

## I. 이 방식이 실패했는지 판정하는 지표

| 지표 | 기준선 (2026-08-07) | 현재 (2026-08-08) | 목표 |
|---|---|---|---|
| JSON-LD `dateModified` stale | **78%** (89/114) | **0%** — 91편 일괄 교정 + 훅으로 고정 | 0% |
| 테제 미갱신 페이지 | 표본 **5/5** | **0/16** — 핫 6편 + Tier 1 10편 완료 | 0 수렴 |
| 판정 안 된 트리거 | **13개** | **0** (K3 강등·K4 폐기를 페이지에도 반영) | 0 |
| 미정정 잔존 | **6건** | **0** | 0 |
| 상단 "현재 판단" 보유 | — | **16편** / 잔여 Tier 2 6편 · Tier 3 21편 | 손댈 때 적용 |

**4주 뒤 이 숫자들이 개선되지 않으면 이 방식이 실패한 것이고, 그때 구조 도입을 다시 검토한다.**

### 판정 시 주의 — 숫자가 좋아진 것과 방식이 작동한 것은 다르다

기준선 4개가 모두 0으로 갔지만, 이것은 **한 번의 집중 작업으로 밀어낸 결과**다. 이 방식이
실제로 작동하는지는 **다음 정보가 들어왔을 때** 판정된다. 진짜 판정 지표는 이것이다.

- **새 사실이 들어왔을 때 본문이 고쳐지는가, 아니면 아래에 블록이 하나 더 붙는가.**
  Tier 1에서 반복해서 발견한 실패 양상이 정확히 이것이었다 — 편집자 주에 "본문 기준가가
  낡았다", "R3의 전제가 흔들렸다"라고 **적어만 두고 본문은 그대로** 두는 것. 경고를 적는
  순간이 곧 고칠 순간이다.
- **"최근 변경 3~5건" 상한이 지켜지는가.** 이 상한이 페이지 길이 상한을 만든다. 6건째를
  붙이고 싶어지면 그것은 본문에 병합할 내용이라는 뜻이다.

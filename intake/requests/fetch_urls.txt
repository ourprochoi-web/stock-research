# 주문형 URL 수집 요청 — 이 파일을 고쳐 push 하면 .github/workflows/collect-fetch.yml 이 돈다.
# 결과: intake/files/fetch/{날짜}/{label}.{ext} + intake/collected.jsonl 한 줄(routed:false).
# 형식: label | https://url   (label 생략 가능 · '#' 주석 · https 만)
# 왜 있나: 세션 샌드박스의 egress 정책에 막힌 호스트를 사용자 소유 CI 러너에서 받기 위해서다(§W5 · §H1).
#   프록시 우회가 아니다 — 세션의 403 은 조직 정책이고 여기서 쓰는 건 시세 봇이 쓰는 것과 같은 길이다.
# 📌 근본 해결은 환경 allowlist 다(사용자 결정 2026-09-20) — 목록·경로는 docs/method_datasource.md 맨 뒤.
#   allowlist 가 적용되면 반복 구조화 출처(SEC·네이버·DART·FRED)는 세션에서 바로 읽고,
#   이 파일은 allowlist 에 넣을 수 없는 일회성 페이지(언론 기사·정부 발표 첨부)만 남는다.
# 처리 끝난 요청은 지운다(git 이 기억한다). 지우지 않으면 다음 push 때 또 받아 id 만 늘어난다.

# ── 완료분(2026-09-20 · 10/10 성공 → C1 정정으로 라우팅됨) 은 지웠다.
#    남은 것 하나 — 대미투자 설명자료 <본문>. HTML 엔 제목·날짜(2026-09-15 산업통상부)만 있고 본문은 첨부에 있다.
#    ⚠ 첨부 직링크를 아직 못 찾았다(HTML 에 '바로보기/내려받기' 스크립트만 있다) — URL 을 확인한 뒤 여기 적는다.

# ── 완료분은 지운다. 대미투자 설명자료 본문(fileId=198552978 PDF · 198552979 HWPX)은
#    2026-09-20 run 35486207633 에서 받았다 → intake/user.jsonl u-20260920-08.
#    교훈 ①: /common/download.do 는 브라우저 UA + 쿠키 + Referer 가 있어야 응답을 끝낸다
#       (collect_fetch.py BROWSER_HOSTS). 1차 run 35485990057 은 이것 때문에 Fetch 가 10분을 넘겼다.
#    교훈 ②: 워크플로 버그도 같이 고쳤다(5a454ac) — 1차 런은 push 가 거절돼도 재시도가 없었다.
#       Fetch 실패와 push 실패가 겹쳐 두 번 잃을 뻔했다.

# ── 2026-09-20 (2) DART 세그먼트 원문 — 세션에서 dart.fss.or.kr·opendart 가 CONNECT 403(조직 정책)으로 닫혔다.
#    같은 날 오전엔 열려 있었다(allowlist 는 세션 단위라 세션 밖에서 보장되지 않는다 · §W5 호스트 기록).
#    막힌 것이 「세그먼트」라 등급이 ②에서 안 올라가는 지점이 그대로 재발했다 — 두 판단이 여기 걸려 있다.
#
#  ① 해성디에스 — 2Q26 영업이익 +204%(82억 → 249억 ①)인데 YTD +1.6%. 「이익이 왔는데 값이 안 붙었다」는 ①이지만
#     그 이익이 AI 패키지기판에서 왔는지 자동차·전력반도체 리드프레임에서 왔는지 <귀속>이 미측정이다.
#     이게 갈리기 전에는 「AI 종목」으로 부르지 않는다. 아카이브 판단 0편.
#
#  ② 두산 — 「주가 매력도 매우 높음」(메리츠 09-18 ③)이 <두산에너빌리티 지분율>이라는 단일 미측정 숫자에 걸린다.
#     30% 가정 시 잔여시총/ex-에너빌 OP 15.9배 · 35% 가정 시 9.7배 — 5%p 로 결론이 뒤집힌다.
#     동시에 전자BG 세그먼트 매출·OP 가 있어야 CCL 증설 9,684억(중국 56.5%)의 분모가 생긴다.

# ── 2026-09-20 (3) 반도체 <소재> 층 티커 검증 — 이세철 구간론(③ 6/17)의 3번째 구간을 볼 장치가 없다.
#    WATCH 195종목·스냅샷 225개인데 반도체 소재가 0개다(솔브레인·동진쎄미켐·후성 등 17/17 미등록).
#    스냅샷에는 회사명 필드가 없어 <틀린 코드는 조용히 다른 회사를 받아온다> — 등록 전에 이름을 대조한다.
#    세션에서 m.stock.naver.com·api.stock.naver.com 이 CONNECT 403(조직 정책)이라 러너로 돌린다.
#    받은 뒤 stockName 을 대조해 맞는 것만 update_prices.py WATCH 에 넣는다.

# ── 2026-09-20 (4) 후공정 5사 세그먼트 — 오늘 세 번 틀린 원인이 「배수 먼저, 원문 나중」이었다. 순서를 뒤집는다.
#    09-18 원장(①)에서 후공정 13사 중앙 이익 도착률 88% · OP YoY +86% 인데 중앙 PER 26.9 로
#    전공정(도착률 44% · OP −25% · PER 51.4)보다 싸다. 「왜 이익이 왔나」가 미측정이라 판단을 못 세운다.
#
#  ① 심텍 — OP 55→124→103→137→629억(2Q26 은 1Q 의 4.6배 · OPM 12.2%)인데 TTM PER 이 −105.45 다.
#     원인은 4Q25 순손실 −1,168억. 그것이 일회성(손상·소송·평가손)이면 배수가 통째로 달라진다.
#     → 그 손실의 정체와 패키지기판(FC-BGA/MSAP) 매출 구성이 필요하다.
dart | 심텍 | 20260814 | 반기보고서 | 사업의 내용,매출 및 수주,부문,기타손익,손상
#
#  ② 대덕전자 — OP 19→244→289→513→703억으로 5분기 연속 상승(37배). OPM 17.5% · PER 52.03 · 3M −34.0%.
#     후공정 13사 중 가장 깨끗한 우상향인데 그 출처가 FC-BGA 인지 일반 PCB 인지 안 갈린다.
dart | 대덕전자 | 20260814 | 반기보고서 | 사업의 내용,매출 및 수주,부문
#
#  ③ 이오테크닉스 — 2Q26 OPM 30.6% 인데 아카이브 판단 0편이다. 레이저 장비의 어느 제품이 버는지 모른다.
dart | 이오테크닉스 | 20260814 | 반기보고서 | 사업의 내용,매출 및 수주,부문
#
#  ④ 티에스이 — 도착률 192% · OPM 27.8% · PER 25.84 로 질과 값이 둘 다 좋은데 판단 3편뿐이고 YTD +321.9% 다.
#     프로브카드 비중과 고객 집중도가 있어야 「이미 갔다」인지 「더 간다」인지 갈린다.
dart | 티에스이 | 20260814 | 반기보고서 | 사업의 내용,매출 및 수주,부문
#
#  ⑤ 디아이 — OP 43→189→396억 · OPM 23.5% · 추정PER 16.28 · 6M −19.0%. 번인 테스트 장비.
#     4Q25 순손실 −103억이 TTM PER 24.92 를 물고 있다 — 정체 확인 필요.
dart | 디아이 | 20260814 | 반기보고서 | 사업의 내용,매출 및 수주,부문,기타손익
